This project integrates Selenium, Java and Cucumber using Maven.
To run the tests, you can execute the TestRunner class. This will trigger the execution of the tests defined in your Cucumber feature files.
